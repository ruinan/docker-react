language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t dockerrui/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true dockerrui/docker-react npm run test -- --coverage